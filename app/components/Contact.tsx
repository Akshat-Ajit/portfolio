"use client";

import { ContactBackground, ContactContent } from "./contact";

export default function Contact() {
  return (
    <section id="contact" className="relative">
      <ContactBackground />
      <ContactContent />
    </section>
  );
}
